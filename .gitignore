.claude/

# Sibling ebook repos (their own git history, deployed separately)
ninja/
slowdown/
mean/
dao/

.DS_Store
# Sensitive / private material — never commit (PII scans, copyrighted PDFs, local drafts)
_dao-references/
.wrangler/
.private-dao/
dao-internal/
.archon/

# env files
.env
.env.*
!.env.example
